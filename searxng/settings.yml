use_default_settings: true

server:
  # Override with SEARXNG_SECRET_KEY env var in Railway
  secret_key: "changeme-set-SEARXNG_SECRET_KEY-in-railway"
  limiter: false
  image_proxy: false

search:
  formats:
    - html
    - json
